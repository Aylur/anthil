{
  "name": "anthil",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r --parallel run test"
  },
  "prettier": {
    "semi": false,
    "proseWrap": "always"
  },
  "devDependencies": {
    "@types/react": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
